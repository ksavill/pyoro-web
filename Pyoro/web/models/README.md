Place trained browser policy files in this directory.

Expected filenames:

- `pyoro1-agent.json`
- `pyoro2-agent.json`

If a model file is missing, the web UI will keep AI play unavailable for that mode.
